int main(int argc, char *argv[])
{
  // mechanism to pass it a file
  // functions to draw nano like interface
  /// keybindings in while loop
  //getline for user input here 
  //ofstream to write to a file here
  return 0;
}
